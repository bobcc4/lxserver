## v1.5.8 (2026-08-18)

### 安全维护

- 升级 `vm2` 至 `3.11.6`，修复多个沙箱逃逸、宿主机引用泄漏和内存限制绕过漏洞。
- 锁定传递依赖 `nanoid` 至 `3.3.18`，修复高危安全告警。
- 完整 `npm audit` 清零。

### 升级说明

- 本版本仅包含安全依赖维护，可以从 `v1.5.7` 直接升级。
- Electron、TypeScript、构建工具链和 `music-tag-native` 的升级留到 `v1.6.0`，不在本版本混入。
