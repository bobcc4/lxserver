## v1.5.4 (2026-08-12)

### 歌词与缓存修复

- 默认歌词输出调整为：外置逐行 LRC、内嵌增强型 LRC（ESLyric）。重置设置或首次使用时会采用该组合；已有用户自行保存的歌词格式设置保持不变。
- 修复普通同步账户播放歌曲时，后台缓存请求误携带管理员专用的命名规则参数，导致音频下载被服务端拒绝、仅留下歌词文件的问题。
- 后台缓存请求失败现在会保留明确的错误信息，便于从浏览器控制台和服务端日志定位下载失败原因。

### 依赖安全维护

- 将 `js-yaml` 升级至 `4.3.1`，将传递依赖 `nanoid` 升级至 `3.3.17`。
- `npm audit` 已无已知漏洞；未进行框架或大版本依赖升级，避免扩大维护版本的变更范围。

### 升级说明

- 本版本不改变数据目录、数据库或接口结构，可以从 `v1.5.3` 直接升级。
