## v1.5.2 (2026-08-09)

### 日志系统完善

- 应用运行期的普通、警告和错误输出统一写入应用日志，业务异常保留完整堆栈并同步进入错误日志。
- 访问日志新增 HTTP 状态码、请求耗时、请求与响应大小、来源 IP 和客户端信息，`5xx` 请求同步进入错误日志。
- 登录日志补充用户、来源 IP 和客户端信息，Token 配置操作补充来源 IP，便于定位异常访问和账户问题。
- 密码、Cookie、Token、签名及 URL 内嵌凭据自动脱敏；启动日志不再输出用户密码。
- 应用、访问、登录、Token 和错误日志统一按 10 MB 轮转并保留 10 个历史文件，避免日志无限增长。
- 限制后台日志接口仅能读取已定义的日志类型，并将单次读取范围限制在 1 至 2000 行。

### 界面修复

- 修复浏览器处于非 100% 缩放比例时，切换到访问日志后超长 URL 撑宽页面并造成异常缩放的问题。
- 日志长文本现在会在容器范围内自动换行，应用日志和登录日志继续沿用原有布局。

### 升级说明

- 本版本不改变持久化目录和数据结构，可从 `v1.5.1` 直接升级。
