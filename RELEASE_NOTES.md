## v1.5.3 (2026-08-11)

### 逐字歌词

- TX 歌词优先通过原生 QRC 接口获取并在服务端完成纯 TypeScript 解密，支持逐字时间轴、翻译和罗马音；QRC 不可用时仅降级到 TX 自身的逐行 LRC，不进行跨平台歌词匹配。
- 保留 WY YRC、KG KRC 和 MG MRC 的原生逐字歌词能力；缺少逐字数据时明确降级为同平台逐行歌词，不生成伪造的逐字时间戳。
- 修复服务器下载链路过早把结构化歌词压成纯文本、导致逐字歌词和扩展歌词在保存前丢失的问题。

### 歌词格式

- 设置新增“外置歌词格式”和“内嵌歌词格式”，可分别选择逐行 LRC、逐字 LRC 或增强型 LRC（ESLyric）。
- 服务器下载队列会持久化每个任务的歌词格式，关闭浏览器后继续下载时仍保持原选择。
- 缓存索引记录请求格式、实际格式和降级原因；缓存列表会显示实际保存的歌词格式，发生降级时使用醒目标记并给出原因。
- 浏览器下载、服务器缓存、仅下载模式、API v1 下载和本地音乐批量嵌入均使用同一套格式转换逻辑。

### 升级说明

- 本版本不改变目录结构，可从 `v1.5.2` 直接升级；已有歌词文件继续兼容，新格式设置对新下载或重新补全的歌词生效。
