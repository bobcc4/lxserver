## v1.5.7 (2026-08-18)

### 管理后台与用户角色修复

- 恢复管理后台页面路径自定义功能，默认路径仍为 `/admin`，支持在设置中修改为自定义路径。
- 管理页面路径增加保留路径校验，不改变 `/api/v1/admin/*`、播放器根路径和 Subsonic 接口。
- 修复管理员将用户名从 `admin` 修改为其他名称后被识别为普通用户的问题。
- 用户角色持久化为 `isAdmin` 字段，旧数据会在启动时自动迁移。

### 升级说明

- 本版本可以从 `v1.5.6` 直接升级，不需要删除数据目录或重建数据库。
