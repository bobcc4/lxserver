## v1.3.4 (2026-08-02)

### Bug 修复

- **TX 歌手搜索**：兼容 QQMusic 主搜索接口的数组、`list`、`itemlist`、`items` 及嵌套结果；主接口空结果时使用 Smartbox 回退。
- **Windows 客户端歌词**：本地歌词解析结果为对象时统一输出字符串 `content`，避免客户端把对象解析为 `[object Object]` 后显示无歌词。
- **Subsonic 最近发行**：按专辑完整发行日期排序；旧平台 ID 无法查询时，按专辑名和歌手跨 TX/WY 曲库补全日期，并持久化查询缓存。
- **Subsonic 曲库数量**：歌曲库和扫描数量只统计 `music` 目录中的物理文件，歌单中尚未下载的在线歌曲不再重复计入媒体库，但仍保留在原歌单中。

### Windows 客户端支持

- 歌单响应会关联同一首本地歌曲，返回实际音质、容器、大小、码率和本地流标识。
- 本地曲库时长为 `mm:ss` 或 `hh:mm:ss` 时也能根据文件大小计算实际码率。
- 本地曲库及歌单封面提供与歌曲身份绑定的短期签名地址，客户端无需暴露访问令牌即可安全显示封面。

### 兼容性

- 本版本与音云 Windows 客户端 `v1.0.3` 配套使用。
- 数据目录结构与 `v1.3.3` 保持兼容，可直接升级。
